<UserControl x:Class="TcpFileMover.Views.Send"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Label DockPanel.Dock="Left">File</Label>
            <TextBox Text="{Binding FilePath}" MinWidth="200"></TextBox>
            <Button Click="BrowseForFile">Browse</Button>
        </StackPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Label>Destination:</Label>
            <TextBox Text="{Binding DestinationString}" MinWidth="100"></TextBox>
            <Label>Port:</Label>
            <TextBox Text="{Binding PortString}" MinWidth="50"></TextBox>
        </StackPanel>
        <Button DockPanel.Dock="Top" Click="SendFile">Send</Button>
        <TextBlock TextWrapping="WrapWithOverflow" Text="{Binding EventText}"></TextBlock>
    </DockPanel>
</UserControl>
