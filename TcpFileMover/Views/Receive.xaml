<UserControl x:Class="TcpFileMover.Views.Receive"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Label>Port:</Label>
            <TextBox Text="{Binding PortString}" MinWidth="50"></TextBox>
            <Button DockPanel.Dock="Top" Click="ReceiveFile">Receive</Button>
        </StackPanel>
        <TextBlock TextWrapping="WrapWithOverflow" Text="{Binding EventText}"></TextBlock>
    </DockPanel>
</UserControl>
